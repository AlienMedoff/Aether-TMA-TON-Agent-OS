version: '3.8'
services:
  runtime:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
    restart: always
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
    restart: always
